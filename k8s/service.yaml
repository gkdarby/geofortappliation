apiVersion: v1
kind: Service
metadata:
  name: geofortapplication-service
  namespace: geofort-app
spec:
  type: NodePort
  selector:
    app: geofortapplication
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30088
